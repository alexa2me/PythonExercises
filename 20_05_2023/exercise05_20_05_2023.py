# Create a program that reads 2 numbers and returns the average between them

grade1 = float(input('Type the first grade: '))
grade2 = float(input('Type the second grade: '))

print('Your average is {:.2f}'.format((grade1+grade2)/2))
