# Create a program that reads a number and shows its successor and predecessor

number = int(input('Type a number: '))

print('The successor is {} and the predecessor is {}.'.format(number+1, number-1))
