# Create an algorithm that reads the temperature in °C and returns the conversion to °F.

temperature = float(input('What is the temperature in °C? '))

print('The new temperature in Fahrenheit is {:.0f}°F.'.format((temperature*1.8)+32))

